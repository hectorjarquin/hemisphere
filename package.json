{
  "name": "hemisphere",
  "version": "2.0.2",
  "description": "Lightweight memory MCP server with hybrid FTS + vector search",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hectorjarquin/hemisphere.git"
  },
  "main": "index.js",
  "bin": {
    "hemisphere": "dashboard.js"
  },
  "scripts": {
    "start": "node dashboard.js",
    "stop": "node dashboard.js stop",
    "restart": "node dashboard.js restart",
    "backup": "node -e \"import('./db.js').then(m=>{const r=m.doBackup();console.log(r.filename);process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^11.0.0",
    "sqlite-vec": "^0.1.9"
  }
}
